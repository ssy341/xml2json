{"dt-api":{"summary":"Get the number of elements that are contained in an API instance","description":"If can sometimes be useful to know how many items are contained in an API's result set - a function that is provided by this method.\r\n\r\n\t\tIt is important to note that the `length` property cannot simply be reliably used itself to determine if the API result set contains any elements or not as the DataTables API is multi-table aware and the result set consists of nested arrays. Therefore we must flatten arrays before being able to use the `length` property. This method does exactly that (i.e. it is a shortcut for `api.flatten().length`).","name":"count()","example":[{"content":"\r\nvar table = $('#example').DataTable();\r\n\r\nalert( 'Rows '+table.rows( '.selected' ).count()+' are selected' );\r\n\r\n","title":"Count how many rows there are with the class `selected`"},{"content":"\r\nvar table = $('#example').DataTable();\r\n\r\nif ( ! table.data().count() ) {\r\n\talert( 'Empty table' );\r\n}\r\n\r\n","title":"Count the number of data entries (i.e. rows) in a table"}],"since":"1.10.8","type":{"description":"Get the number of entries in an API instance's result set, regardless of multi-table grouping (e.g. any data, selected rows, etc).","returns":{"content":"The number of items in the API instance's result set","type":"integer"},"type":"function","signature":"count()"},"group":"utility","related":"dt-api any()"}}